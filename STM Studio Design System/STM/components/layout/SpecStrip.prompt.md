Row of studio facts split by vertical hairlines. Values are words as often as numbers.

```jsx
<SpecStrip specs={[{value:'5.5m',label:'Width'},{value:'4m',label:'Depth'},{value:'Skylight',label:'Natural light'}]} />
```

Don't pad this with invented metrics — it carries only facts that exist in the studio brief.
