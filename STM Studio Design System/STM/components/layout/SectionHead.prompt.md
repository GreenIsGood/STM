Eyebrow + oversized Archivo heading, optionally with a right-hand paragraph.

```jsx
<SectionHead eyebrow="01 — Selected work" title="On the credits."
  aside="Every job below carries our name in the credits." />
```

Headings are short declarative sentences ending in a full stop. Arabic drops the negative tracking and opens the leading to 1.3 — pass `arabic` rather than restyling.
