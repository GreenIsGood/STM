One line of a film credit: mono key, sans value.

```jsx
<CreditRow label="Client" value="KION" />
<CreditRow label="STM" value="Production house" accent />
```

The `STM` row is always last and always `accent` — it's the only red text in the card.
