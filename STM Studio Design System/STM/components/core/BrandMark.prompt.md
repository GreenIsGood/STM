The STM lockup: an italic Archivo "stm" in a disc, optionally with the name and descriptor.

```jsx
<BrandMark size={42} lockup="full" />
<BrandMark size={40} inverted lockup="short" />
```

**No vector logo file was supplied** — this typographic lockup is the mark. Do not redraw, trace or invent an STM logo; if a real file arrives, swap the disc's contents for it. The disc is the only rounded shape in the entire system.
