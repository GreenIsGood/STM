Transparent field with a single hairline underline — the form equivalent of the brand's Rule dividers. Focuses to red.

```jsx
<Input label="Project name" placeholder="e.g. KION — summer campaign" />
<Input label="Brief" placeholder="What are we shooting?" multiline rows={4} />
```

**Intentional addition** — no form field exists in the source mockups. Only use where a real brief/contact form is needed; never as a stand-in for a button or a link.
